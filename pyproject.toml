[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "cssmap2sm64"
version = "0.1.0"
requires-python = ">=3.11"

[project.scripts]
cssmap2sm64 = "cssmap2sm64.__main__:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["cssmap2sm64*"]
